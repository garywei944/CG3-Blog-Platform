<!DOCTYPE html>
<html>
<head>
</head>

<body>


<div class="container">
<h2>Database Results</h2>

<h3>Tables</h3>

<h3><%= JSON.stringify(results) %></h3>
<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.username %> - <%= r.pwd %> - <%= r.profile_pic %> - <%= r.bio %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
